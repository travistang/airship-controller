<ion-view title="Control Panel">
	<ion-content>
	      <div class="list">
		<label class="item item-input item-select">
			<div class="input-label">Device</div>
			<select ng-model="selectedDevice">
				<option ng-repeat="device in deviceList" value="{{device.address}}">{{device.name}}</option>
			</select>
		</label>
			</div>
		
			<button class="button button-dark button-full" color="secondary" ng-click="getDevice()">Refresh</button>
			<button class="{{connectBtnStyle}}" ng-click="connect()">Connect</button>
			<div id="container" style="width:100%;height:100%;background-color:#cccccc"></div>
	</ion-content>
</ion-view>
